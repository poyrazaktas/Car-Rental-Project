import { FilterBrandsPipe } from './filter-brands.pipe';

describe('FilterBrandsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBrandsPipe();
    expect(pipe).toBeTruthy();
  });
});
