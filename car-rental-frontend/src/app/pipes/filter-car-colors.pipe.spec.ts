import { FilterCarColorsPipe } from './filter-car-colors.pipe';

describe('FilterCarColorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCarColorsPipe();
    expect(pipe).toBeTruthy();
  });
});
