import { FilterColorsPipe } from './filter-colors.pipe';

describe('FilterColorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterColorsPipe();
    expect(pipe).toBeTruthy();
  });
});
