import { FilterCarBrandsPipe } from './filter-car-brands.pipe';

describe('FilterCarBrandsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCarBrandsPipe();
    expect(pipe).toBeTruthy();
  });
});
