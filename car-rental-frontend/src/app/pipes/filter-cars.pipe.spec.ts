import { FilterCarsPipe } from './filter-cars.pipe';

describe('FilterCarsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCarsPipe();
    expect(pipe).toBeTruthy();
  });
});
